spring:
  application:
    name: demo-jwt-auth
  datasource:
    url: jdbc:postgresql://localhost:9003/jwtauth_db
    username: postgres
    password: springpg123*
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
